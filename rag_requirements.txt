# Python Dependencies for RAG System

# Core ML/AI
faiss-cpu>=1.7.4
numpy>=1.24.0

# Embeddings (local, free) - CPU only for Railway
sentence-transformers>=2.2.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0+cpu

# HTTP Client for OpenRouter API (LLM only)
requests>=2.31.0

# Backend API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0

# Text Processing (already in OCR requirements but adding for completeness)
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
